org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
kotlin.code.style=official

# Build with Temurin. Oracle's JDK cacerts lacks Sectigo Public Server
# Authentication Root R46, which Unidata's Maven repository chains to, so
# NetCDF-Java cannot be resolved on that JVM. See docs/adr/0006-grib2.md.
# Forward slashes deliberately: a properties file treats backslash as escape.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-23.0.2.7-hotspot
